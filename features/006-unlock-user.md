# Unlock user dialog box

Provide a list of operations in a navigation panel or menu dropdown. These 
operations are meant to be quick and easy to execute synchronously and
will not have a lot of input fields required, so a modal dialog box
should be sufficient. 

The first operation we want to add is one to "unlock user" -- it will take
a username field. Once the submit button is checked, it will execute an 
operation on the backend. 

Please go ahead and add this operation under ./proposed-changes/operations


