# Contract: check-ami-state operation
id: check-ami-state
label: Check AMI State
function: Check whether AMI in destination account is available; used for pipeline polling.
modes:
  default: Read-only. Returns ready true when AMI state is available. No mode parameter.

invocation:
  target: SreManagementFunction
  description: Invoke the SRE management Lambda synchronously; payload includes operation and inputs below.
  method: lambda.invoke
  invocation_type: RequestResponse
  payload:
    required:
      operation: check-ami-state
      ami_id: string
      destination_account_id: string
    optional:
      destination_region: string
      source_region: string
  example_python: "Same pattern as apply-ansible-playbook: boto3.client('lambda').invoke(FunctionName=<SreManagementFunctionArn>, InvocationType='RequestResponse', Payload=json.dumps(payload))."
  response:
    success: object with status, ready, state
    error: object with status, error, message

inputs:
  required:
    - operation
    - ami_id
    - destination_account_id
  optional:
    - destination_region
    - source_region
outputs:
  success:
    - status
    - ready
    - state
  error:
    - status
    - error
    - message
